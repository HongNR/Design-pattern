# 🔁 반복자 패턴 (Iterator Pattern)

> 행동 패턴 (Behavioral Pattern)
> 참고: https://refactoring.guru/ko/design-patterns/iterator

### 🗂️ 목차

1. 반복자 패턴이란?
2. 어떤 문제를 해결하나?
3. 구조와 구성 요소
4. 코드 예시 (Before → After)
5. 한 걸음 더: 순회 방식 바꾸기 / 트리 순회 / 지연 로딩
6. 장단점
7. 실제 사용 예시 & 추가로 알면 좋은 내용
8. 핵심 정리

---

## 1. 반복자 패턴이란?

> **컬렉션의 내부 구조(배열인지, 리스트인지, 트리인지)를 드러내지 않고, 그 안의 요소들을 하나씩 차례대로 꺼내볼 수 있게 해주는 행동 패턴**

한마디로 **"꺼내는 방법은 하나로 통일하고, 보관 방법은 숨긴다"** 입니다.

### 🎵 쉬운 비유 1: 음악 앱의 "다음 곡 ⏭" 버튼

- 멜론, 유튜브 뮤직, 스포티파이… 앱마다 플레이리스트를 **서버에 어떻게 저장하는지** 우리는 전혀 모릅니다.
- 내 앨범이든, 최근 들은 곡이든, 추천 플레이리스트든 상관없이 **`⏭ 다음 곡` 버튼 하나**로 곡을 넘깁니다.
- 더 들을 곡이 없으면 버튼이 비활성화되죠.

| 음악 앱 | 반복자 패턴 |
|---|---|
| 플레이리스트 | 컬렉션 |
| ⏭ 다음 곡 버튼 | `next()` |
| 다음 곡이 있으면 버튼 활성화 | `hasNext()` |
| 서버의 저장 방식 | 숨겨진 내부 구조 |

### 📖 쉬운 비유 2: 책갈피

- 책(컬렉션)을 읽다가 덮을 때 **책갈피**를 꽂아 둡니다.
- 책갈피는 **"어디까지 읽었는지"** 를 기억하고, 다음에 펼치면 **거기서부터 이어서** 읽을 수 있습니다.
- 같은 책을 형과 동생이 읽어도 **각자 책갈피를 따로 꽂으면** 서로 방해하지 않습니다.
- 책 자체는 누가 어디까지 읽었는지 신경 쓰지 않습니다. **읽는 위치 관리는 책갈피의 몫**입니다.

> 여기서 책갈피가 바로 **반복자(Iterator)** 입니다.
> 반복자는 **현재 위치를 스스로 기억**하고, **여러 개를 동시에** 쓸 수 있습니다.

---

## 2. 어떤 문제를 해결하나?

### 문제 1: 컬렉션마다 꺼내는 방법이 다르다

```java
for (int i = 0; i < arr.length; i++)  { arr[i]; }       // 배열
for (int i = 0; i < list.size(); i++) { list.get(i); }  // 리스트
for (String key : map.keySet())       { map.get(key); } // 맵
// 트리라면? → 재귀 + 깊이 우선/너비 우선까지 고민...
```

- **자료구조마다 순회 코드가 전부 다릅니다.**
- 클라이언트는 **"이 데이터가 어떤 구조로 저장되어 있는지"를 반드시 알아야** 합니다.

### 문제 2: 내부 구조가 바뀌면 사용하는 쪽 코드도 전부 바뀐다

- 배열로 만들었다가 `List`로 바꾸면 → **순회하던 모든 코드를 찾아가서 수정**해야 합니다.

### 문제 3: 컬렉션 클래스에 순회 로직을 넣으면 책임이 뒤섞인다

- "순서대로", "거꾸로", "랜덤으로", "깊이 우선" … 전부 컬렉션 안에 넣으면 **"데이터 보관"** 이라는 원래 책임이 흐려집니다.

### 💡 해결책

> **순회하는 일을 컬렉션에서 떼어내어 "반복자"라는 별도 객체에게 맡긴다!**

- 모든 반복자는 **같은 인터페이스**(`hasNext()`, `next()`)를 가집니다.
- 반복자는 **"지금 어디까지 봤는지"** 를 스스로 기억합니다. (책갈피)
- 그래서 클라이언트는 **컬렉션이 무엇이든 같은 코드로** 순회할 수 있습니다.

---

## 3. 구조

```
┌──────────┐
│  Client  │
└────┬─────┘
     │ 인터페이스만 사용
     ├───────────────────────────────┐
     ▼                               ▼
┌─────────────────────┐      ┌──────────────────────┐
│ <<interface>>       │      │ <<interface>>        │
│ IterableCollection  │      │ Iterator             │
├─────────────────────┤      ├──────────────────────┤
│ + createIterator()  │─────▶│ + hasNext(): boolean │
└─────────▲───────────┘ 생성  │ + next(): T          │
          │                  └──────────▲───────────┘
          │ 구현                         │ 구현
┌─────────┴───────────┐      ┌──────────┴───────────┐
│ ConcreteCollection  │◀─────│ ConcreteIterator     │
├─────────────────────┤ 참조  ├──────────────────────┤
│ - 실제 데이터        │      │ - collection         │
│ + createIterator()  │      │ - currentPosition    │
└─────────────────────┘      │ + hasNext() / next() │
                             └──────────────────────┘
```

### 주요 구성 요소

| 구성 요소 | 역할 | Java 표준 라이브러리에서는? |
|---|---|---|
| **Iterator (반복자 인터페이스)** | 순회에 필요한 메서드 선언 (`hasNext`, `next`) | `java.util.Iterator` |
| **Concrete Iterator (구상 반복자)** | 실제 순회 알고리즘 구현 + **현재 위치를 기억** | `ArrayList` 내부의 `Itr` 클래스 |
| **Iterable Collection (컬렉션 인터페이스)** | 반복자를 만들어주는 메서드 선언 | `java.lang.Iterable` (`iterator()`) |
| **Concrete Collection (구상 컬렉션)** | 자신에게 맞는 반복자를 생성해서 반환 | `ArrayList`, `HashSet` 등 |
| **Client (클라이언트)** | 인터페이스만 보고 순회 | 우리가 짜는 코드 |

> **컬렉션은 반복자를 만들어주기만 하고, 순회는 반복자가 한다!**

---

## 4. 코드 예시: 음악 앱 플레이리스트

### 상황

- **내 앨범** : 곡 수가 고정이라 **배열**로 저장
- **최근 들은 곡** : 계속 늘어나서 **ArrayList**로 저장

두 플레이리스트의 곡을 모두 출력해 봅시다.

```java
record Song(String title, String artist) {}
```

---

### ❌ [Before] 반복자 패턴 미적용

```java
class AlbumPlaylist {
    Song[] songs = new Song[10];
    int count = 0;
}

class RecentPlaylist {
    List<Song> songs = new ArrayList<>();
}

// 클라이언트
void printAll(AlbumPlaylist album, RecentPlaylist recent) {
    for (int i = 0; i < album.count; i++) {        // 배열은 이렇게
        System.out.println(album.songs[i]);
    }
    for (int i = 0; i < recent.songs.size(); i++) { // 리스트는 또 이렇게
        System.out.println(recent.songs.get(i));
    }
}
```

**🚨 문제점**
1. 클라이언트가 **배열인지 리스트인지** 내부 구조를 다 알아야 함
2. 플레이리스트 종류가 늘어나면 **순회 코드도 계속 늘어남**
3. 외부에서 `recent.songs.clear()` 처럼 **내부 데이터를 마음대로 조작** 가능

---

### ✅ [After] 반복자 패턴 적용

Java에는 이미 `Iterator`(반복자)와 `Iterable`(컬렉션) 인터페이스가 있으니 그대로 활용합니다.

```java
// Java 표준 인터페이스 (직접 만들 필요 없음)
interface Iterator<T> { boolean hasNext(); T next(); }
interface Iterable<T> { Iterator<T> iterator(); }
```

#### 배열 기반 플레이리스트 → 반복자를 직접 구현

```java
class AlbumPlaylist implements Iterable<Song> {
    private final Song[] songs = new Song[10];
    private int count = 0;

    void add(Song song) { songs[count++] = song; }

    @Override
    public Iterator<Song> iterator() {
        return new Iterator<>() {
            private int position = 0;  // 📖 책갈피: 현재 위치를 기억

            public boolean hasNext() { return position < count; }
            public Song next()       { return songs[position++]; }
        };
    }
}
```

#### 리스트 기반 플레이리스트 → 리스트의 반복자를 그대로 전달

```java
class RecentPlaylist implements Iterable<Song> {
    private final List<Song> songs = new ArrayList<>();

    void add(Song song) { songs.add(song); }

    @Override
    public Iterator<Song> iterator() { return songs.iterator(); }
}
```

#### 클라이언트

```java
// ⭐ 배열이든 리스트든, 앞으로 생길 어떤 구조든 이 메서드 하나로 끝!
void printAll(Iterable<Song> playlist) {
    for (Song song : playlist) {
        System.out.println(song);
    }
}

printAll(album);
printAll(recent);
```

> `for-each` 문은 컴파일러가 아래 코드로 바꿔서 실행합니다. **for-each = 언어가 지원하는 반복자 패턴**입니다.
> ```java
> Iterator<Song> it = playlist.iterator();
> while (it.hasNext()) { Song song = it.next(); ... }
> ```

### 🔍 Before vs After 비교

| 항목 | Before | After |
|---|---|---|
| 클라이언트가 알아야 하는 것 | 배열인지, 리스트인지, 크기는 어떻게 구하는지 | `hasNext()`, `next()` 두 개뿐 |
| 새로운 플레이리스트 추가 시 | 클라이언트 수정 필요 | 클라이언트 수정 없음 (OCP) |
| 내부 저장 방식 변경 시 | 클라이언트 코드도 수정 | 해당 컬렉션 클래스만 수정 |
| 내부 데이터 보호 | 그대로 노출 | 반복자를 통해서만 읽기 가능 |

---

## 5. 한 걸음 더

###  같은 컬렉션, 다른 순회 방식 (역순 / 셔플 재생)

컬렉션은 그대로 두고 **반복자만 갈아끼우면** 순회 방식이 바뀝니다.

```java
class RecentPlaylist {
    private final List<Song> songs = new ArrayList<>();

    Iterator<Song> inOrder() { return songs.iterator(); }

    Iterator<Song> reverse() {
        return new Iterator<>() {
            private int position = songs.size() - 1;
            public boolean hasNext() { return position >= 0; }
            public Song next()       { return songs.get(position--); }
        };
    }

    Iterator<Song> shuffle() {
        List<Song> copy = new ArrayList<>(songs);
        Collections.shuffle(copy);
        return copy.iterator();
    }
}
```

- 사용하는 쪽 코드(`while (it.hasNext()) it.next();`)는 **완전히 동일**합니다.
- 반복자마다 위치를 따로 기억하므로 **여러 반복자가 동시에 순회**해도 서로 방해하지 않습니다. (책갈피 여러 개)

---


## 6. 장단점

### 장점

1. **단일 책임 원칙 (SRP)**
   - "데이터 보관"은 컬렉션이, "데이터 순회"는 반복자가 담당합니다.

2. **개방/폐쇄 원칙 (OCP)**
   - 새로운 컬렉션이나 새로운 순회 방식(셔플, 역순, BFS 등)을 추가해도 **기존 클라이언트 코드는 수정할 필요가 없습니다.**

3. **내부 구조 캡슐화**
   - 데이터가 배열인지, 리스트인지, 트리인지, DB에 있는지 몰라도 되고, 내부 데이터를 직접 조작할 수도 없어 안전합니다.

4. **동시에 여러 순회 가능 / 멈췄다가 이어서 순회 가능**
   - 반복자마다 자기 위치를 따로 기억하기 때문입니다. (책갈피)

### 단점

1. **단순한 컬렉션에는 과한 설계**
   - `List` 하나 순회하려고 반복자를 직접 만드는 건 불필요합니다. (Java 표준 `Iterator`면 충분)

2. **직접 접근보다 느릴 수 있음**
   - `arr[i]` 직접 접근보다 객체 생성과 메서드 호출 비용이 약간 추가됩니다.

3. **순서대로만 접근 가능**
   - "5번째 요소로 바로 점프" 같은 임의 접근(Random Access)에는 적합하지 않습니다.

4. **순회 중 컬렉션 변경 문제**
   - 순회 도중 원본이 바뀌면 예외가 발생할 수 있습니다. (아래 7-3 참고)

---

## 7. 실제 사용 예시 & 추가로 알면 좋은 내용

### 7-1. 실제 사용 사례

| 분류 | 사례 | 설명 |
|---|---|---|
| Java 표준 | `java.util.Iterator`, `Iterable` | 모든 컬렉션(`List`, `Set`, `Queue`…)의 순회 기반 |
| Java 표준 | `for-each` 문 | `Iterable`을 구현한 객체라면 무엇이든 순회 |
| Java 표준 | `java.util.Scanner` | `hasNext()`, `next()`로 입력을 토큰 단위로 순회 |
| Java 표준 | `java.util.Enumeration` | `Vector`, `Hashtable`에서 쓰던 옛날 반복자 |
| JDBC | `ResultSet` | `while (rs.next())`로 조회 결과를 한 행씩 순회 |
| Spring Data | `Page`, `Slice`, `Stream<T>` | 대용량 조회 결과를 나눠서 순회 |
| Spring Batch | `ItemReader` | `read()`를 반복 호출하며 데이터를 하나씩 읽음 (null이면 종료) |
| 다른 언어 | Python `for ... in`, `yield` / JavaScript `for...of` | 언어 문법 자체에 반복자 패턴이 녹아있음 |

### 7-2. 외부 반복자 vs 내부 반복자

| 구분 | 외부 반복자 (External) | 내부 반복자 (Internal) |
|---|---|---|
| 누가 순회를 제어? | **클라이언트**가 `next()`를 직접 호출 | **컬렉션**이 알아서 돌면서 동작만 받음 |
| 예시 | `while (it.hasNext()) it.next();` | `list.forEach(System.out::println);` |
| 장점 | 중간에 멈추기 등 **세밀한 제어** 가능 | 코드가 **짧고 간결** |

> Java 8의 **Stream API**도 내부적으로 `Spliterator`라는 "분할 가능한 반복자"를 사용합니다.

### 7-3. 주의: 순회 중에 컬렉션을 수정하면? (Fail-Fast)

```java
for (String s : list) {
    if (s.equals("B")) list.remove(s);  // 💥 ConcurrentModificationException
}

list.removeIf(s -> s.equals("B"));      // ✅ 안전한 방법 (또는 iterator.remove())
```

- `ArrayList`의 반복자는 순회 중 컬렉션이 **반복자 모르게 변경**되면 즉시 예외를 던집니다. 이를 **Fail-Fast** 라고 합니다.
- 잘못된 데이터를 조용히 처리하는 것보다 **빨리 실패하는 게 안전**하다는 설계 철학입니다.
- `CopyOnWriteArrayList`, `ConcurrentHashMap` 같은 동시성 컬렉션은 **Fail-Safe** 방식이라 예외가 나지 않습니다.

### 7-4. 다른 패턴과의 관계

| 패턴 | 관계 |
|---|---|
| **복합체 (Composite)** | 복합체 트리를 순회할 때 반복자를 사용 |
| **팩토리 메서드 (Factory Method)** | `iterator()` 자체가 팩토리 메서드. 컬렉션마다 알맞은 반복자를 생성 |
| **메멘토 (Memento)** | 반복자의 현재 위치를 저장해 두었다가 나중에 되돌릴 수 있음 |
| **비지터 (Visitor)** | 반복자로 요소를 하나씩 꺼내고, 비지터로 각 요소에 작업을 수행 |

---

## 8. 핵심 정리

```
반복자 패턴 = 컬렉션의 '다음 곡 ⏭ 버튼' + '책갈피'

[클라이언트] ──hasNext()/next()──▶ [반복자] ──▶ [컬렉션 (배열/리스트/트리/DB...)]

- 목적: 내부 구조를 숨긴 채, 요소를 하나씩 꺼내기
- 핵심: 컬렉션은 반복자를 "만들어주기만", 순회는 반복자가 "알아서"
- 효과: 어떤 자료구조든 같은 코드로 순회 / 순회 방식 교체 가능
- 우리가 이미 쓰고 있는 것: for-each, Iterator, ResultSet, Stream
```
