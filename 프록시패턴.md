# 프록시 패턴 (Proxy Pattern)
### 🎯 한 문장 요약
> **"실제 객체 앞에 '대리인'을 세워, 직접 만나기 전에 중간에서 일을 처리하는 패턴"**

---

## 1. 비유 🏢

회사 사장님 :  **실제 객체**
\
비서 : **프록시**

```
방문객  →  [비서]  →  사장님
```

비서가 사장님을 만나는 **전후로** 하는 일 (각각 프록시의 한 종류)

| 프록시 종류 | 손님이 오면… (전)             | 사장님이 답을 주면… (후)        |
|---------------|-------------------------|------------------------|
| **보호 프록시** | 약속 확인 **없으면 돌려보냄**      | (통과한 사람만 답 전달)         |
| **캐싱 프록시**  | 전에 온 질문이면 **비서가 바로 답**  | 새 답이면 **적어뒀다가 다음에 재활용** |
| **로깅 프록시** | "누가 왔다" 방명록 기록          | "어떤 답을 받았다" 기록         |
| **가상 프록시** | 사장님 출근 전이면 **안 깨움 기다림** | (깨운 뒤 그대로 전달)          |
| **원격 프록시** | 출장중이면 **전화 연결**         | 전화로 받은 답 **포장해서 전달**   |

> 👉 핵심: 프록시는 실제 객체를 부르기 **전(前)에도**, 부르고 **난 후(後)에도** 일을 합니다.
> `(전처리) → 사장님 호출 → (후처리)` 이렇게 호출을 **앞뒤로 감싸는 거죠.**

> 👉 그리고 **손님은 비서랑 얘기하든 사장님이랑 얘기하든 똑같이 느낍니다.**
> 이게 프록시의 또 다른 핵심입니다. "겉모습은 똑같은데, 중간에서 일을 더 해준다."

---

## 2. 구조 (그림 한 장) 🧩

```
        ┌─────────────────────────────┐
        │   Subject (공통 인터페이스)   │   ← 둘 다 같은 모습
        │        request()            │
        └─────────────────────────────┘
              ▲                ▲
              │                │
     ┌────────────────┐  ┌────────────────────┐
     │   RealSubject  │  │      Proxy         │
     │   (실제 객체)   │  │     (대리인)        │
     │   request()    │◄─│  ① 전처리           │
     │   ※ 진짜 로직   │  │  ② real.request()  │
     └────────────────┘  │  ③ 후처리           │
                         └────────────────────┘
```

**핵심 3가지** (이것만 기억하면 됩니다)
1. **같은 인터페이스**를 구현한다 → 클라이언트는 프록시인지 모른다
2. **실제 객체를 참조**로 들고 있다
3. 호출을 **가로채서 전/후 처리**를 추가한다

---

## 3. 동작 흐름 🔄

```
클라이언트 ──→ 프록시 ──→ 실제 객체
              │  ↑           │
              │  └───────────┘
              │     결과
              │
              ├── (전) 권한 체크 / 캐시 확인 / 로깅
              └── (후) 결과 캐싱 / 로깅
```

**예: 캐싱 프록시**
```
요청 → 캐시에 있나? ──YES──→ 캐시 그대로 반환 (빠름!)
            │
           NO
            │
       실제 객체 호출 → 결과를 캐시에 저장 → 반환
```

---

## 4. 프록시 종류 (5가지) 📦

| 종류 | 무슨 일을 하나? | 실생활 예시 |
|------|----------------|------------|
| **가상 프록시** | 무거운 객체를 필요할 때만 생성 | 이미지 Lazy Loading |
| **보호 프록시** | 권한 있는 사람만 통과 | 관리자만 삭제 가능 |
| **캐싱 프록시** | 같은 요청은 저장해둔 답 재사용 | API 응답 캐시 |
| **로깅 프록시** | 호출 기록을 남김 | 디버깅, 모니터링 |
| **원격 프록시** | 멀리 있는 객체를 가까이 있는 것처럼 | REST API 클라이언트 |

---

## 5. 코드로 보기  💻

```java
// ① 공통 인터페이스 — 실제 객체와 프록시가 똑같이 생겼다
interface UserService {
    User getUser(int id);
}

// (참고) 유저 데이터 클래스
record User(int id, String name) {}

// ② 실제 객체 — DB에서 진짜로 가져옴 (느림)
class RealUserService implements UserService {
    @Override
    public User getUser(int id) {
        System.out.println("[DB] 유저 " + id + " 조회 중...");
        return new User(id, "User_" + id);
    }
}

// ③ 프록시 — 똑같은 인터페이스, 하지만 캐싱을 추가
class UserServiceProxy implements UserService {
    private final Map<Integer, User> cache = new HashMap<>();
    private final UserService real = new RealUserService();

    @Override
    public User getUser(int id) {
        if (cache.containsKey(id)) {
            System.out.println("[Cache] 캐시에서 반환: " + id);  // ← DB 안 감
            return cache.get(id);
        }
        User user = real.getUser(id);   // ← 처음 한 번만 DB
        cache.put(id, user);
        return user;
    }
}

// ④ 사용 — 클라이언트는 프록시인지 실제인지 모른다!
public class Main {
    public static void main(String[] args) {
        UserService service = new UserServiceProxy();
        service.getUser(1); // [DB] 유저 1 조회 중...
        service.getUser(1); // [Cache] 캐시에서 반환: 1  ← 두 번째는 빠름!
    }
}
```

---

## 6. 사례 🌐

프록시는 어려운 이론이 아니라, **Java/Spring 어디에나 이미 깔려 있습니다.**

| 사례 | 어떤 프록시? |
|------|------------|
| **Spring AOP** | `@Transactional`, `@Cacheable` 등이 메서드 호출을 가로채 부가기능 실행 (전/후처리) |
| **JPA / Hibernate Lazy Loading** | `user.getOrders()` 호출하는 순간에야 DB 쿼리 실행 (가상 프록시) |
| **Spring Security** | 메서드 실행 전에 권한을 검사해 막거나 통과 (보호 프록시) |
| **`java.lang.reflect.Proxy`** | 인터페이스만 있으면 런타임에 프록시 객체를 자동 생성 (동적 프록시) |
| **Nginx 리버스 프록시** | 클라이언트는 Nginx와만 통신, 실제 WAS는 뒤에 숨음 (보안 + 로드밸런싱) |

```
[Spring @Transactional 동작 예시]

호출하는 코드 → [Spring이 만든 프록시] → 실제 서비스 객체
                    │  ↑                    │
                    │  └────────────────────┘
                    ├── (전) 트랜잭션 시작
                    └── (후) 커밋 / 롤백

→ 우리가 만든 메서드는 비즈니스 로직만 적었는데,
  프록시가 트랜잭션 처리를 몰래 끼워 넣어준다!
```

> 💡 Spring의 `@Transactional`, `@Cacheable`, `@Async`가 동작하는 원리가 바로 **프록시 패턴**입니다.
> Spring이 우리 객체를 감싼 프록시를 대신 만들어서 주입해주는 거죠.

---

## 7. 언제 쓰면 좋을까? ✅

- 같은 DB/API 요청이 **반복**된다 → 캐싱 프록시
- 실행 전에 **권한 체크**가 필요하다 → 보호 프록시
- 모든 호출을 **기록**하고 싶다 → 로깅 프록시
- 객체 생성이 **무겁고 항상 쓰진 않는다** → 가상 프록시
- **외부/원격 객체**를 로컬 함수처럼 쓰고 싶다 → 원격 프록시

---

## 8. 장점 vs 단점 ⚖️

### 👍 장점
| 장점 | 설명 |
|------|------|
| **기존 코드 수정 없음** | 실제 객체와 클라이언트를 **건드리지 않고** 기능을 추가 (OCP 원칙) |
| **관심사 분리** | 비즈니스 로직(실제 객체)과 부가 기능(캐싱·권한·로깅)을 **분리** |
| **클라이언트는 모름** | 같은 인터페이스라서 프록시를 끼워도 사용하는 코드가 안 바뀜 |
| **유연한 제어** | 접근 차단, 지연 생성 등 호출 자체를 통제 가능 |

### 👎 단점
| 단점 | 설명 |
|------|------|
| **클래스 증가** | 실제 객체마다 프록시 클래스가 따로 필요 → 코드량·복잡도 증가 |
| **응답 지연 가능** | 호출마다 프록시를 한 번 거치므로 약간의 오버헤드 발생 |
| **구조 파악 어려움** | 실제로 어디서 일이 처리되는지 한눈에 안 보임 (디버깅 난이도 ↑) |

> 💡 그래서 Java/Spring은 이 단점(클래스 일일이 만들기)을 **동적 프록시**로 해결합니다.
> `java.lang.reflect.Proxy`나 CGLIB(Code Generator Library)가 **런타임에 프록시를 자동 생성**해주죠. (Spring AOP의 원리)

### 📌 '동적 프록시'?

지금까지 본 방식(5번 코드)은 **프록시 클래스를 손으로 직접 만들었어요.(정적 프록시)**
그런데 서비스가 100개면 **프록시 100개를 일일이 만들어야** 합니다. (= 위의 '클래스 증가' 단점)

**동적 프록시**는 이걸 자동화한 겁니다.
> "미리 만들지 말고, 프로그램이 **실행되는 중(런타임)에 프록시를 자동으로 찍어내자"**
> — 붕어빵 틀 하나로 여러 개 찍어내듯이.

| 방식 | 누가 프록시를 만드나? |
|------|---------------------|
| 정적 프록시 (5번 코드) | **개발자가 직접** 클래스 작성 |
| 동적 프록시 | **실행 중에 자동 생성** (`java.lang.reflect.Proxy` / CGLIB) |

그래서 `@Transactional`만 붙이면, **Spring이 뒤에서 동적 프록시로 프록시 객체를 자동으로 만들어** 트랜잭션을 끼워 넣어줍니다. 
우리는 프록시 코드를 한 줄도 안 쓰죠. → 이게 **Spring AOP의 원리**입니다.

---

## 9. 다른 패턴과의 관계 🔗

프록시는 **구조가 비슷한 패턴들**이 있어서 자주 헷갈립니다. 핵심은 **"목적이 무엇이냐"**로 구분합니다.

| 패턴 | 구조 | **목적 (핵심 차이)** | 한 줄 비유 |
|------|------|--------------------|-----------|
| **프록시** | 같은 인터페이스를 감쌈 | **접근 제어** (캐싱·권한·지연) | "사장님 만나기 전 거치는 **비서**" |
| **데코레이터** | 같은 인터페이스를 감쌈 | **기능 추가** (계속 덧씌움) | "옷 위에 **외투**를 겹쳐 입기" |
| **어댑터** | 인터페이스를 **바꿔** 감쌈 | **호환** (안 맞는 걸 맞게) | "110V 기기를 220V에 꽂는 **돼지코**" |
| **퍼사드** | 여러 객체를 **묶어** 감쌈 | **단순화** (복잡한 걸 쉽게) | "버튼 하나로 다 켜지는 **리모컨**" |

### 특히 헷갈리는 둘만 콕 집으면

- **프록시 vs 데코레이터**
  - 구조는 거의 똑같다 (둘 다 같은 인터페이스로 감쌈).
  - **프록시 = "쓸지 말지/언제 쓸지" 제어**가 목적 (접근 자체를 통제).
  - **데코레이터 = "기능을 더 얹는 것"**이 목적 (감싸고 또 감싸서 기능 누적).

- **프록시 vs 어댑터**
  - **프록시 = 인터페이스가 같다** (겉모습 유지, 클라이언트는 모름).
  - **어댑터 = 인터페이스를 바꾼다** (안 맞는 걸 맞게 변환하는 게 목적).

> 💡 한 마디로: **구조가 닮았어도 "왜 감쌌는가(목적)"가 다르면 다른 패턴**입니다.

---

## 10. 마무리 한 줄 🎬

```
프록시 패턴 = 실제 객체 앞에 '중간 레이어'를 두는 것

· 겉모습(인터페이스)은 똑같다 → 클라이언트는 모른다
· 그 사이에서 캐싱 · 권한 · 로깅 · 지연로딩을 몰래 해준다
· 그래서 Spring AOP, JPA Lazy Loading, Spring Security가 전부 이걸 쓴다
```
