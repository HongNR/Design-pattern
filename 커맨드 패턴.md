# 📄 커맨드 패턴 (Command Pattern) 완벽 가이드

> **요청(Request)을 독립적인 객체로 캡슐화**하여 요청 매개변수화, 작업 대기열(Queue) 저장, 로깅, 그리고 실행 취소(Undo)를 가능하게 하는 행동 디자인 패턴입니다.

---

## 1. 개요 (Overview)

* **패턴 유형:** 행동 패턴 (Behavioral Pattern)
* **다른 이름:** 액션(Action), 트랜잭션(Transaction)
* **핵심 목표:** 요청을 발송하는 객체(**Invoker**)와 실제 비즈니스 로직을 수행하는 객체(**Receiver**) 사이의 **결합도(Coupling)를 완벽히 해제**하는 것.

---

## 2. 구성 요소 (Components)

| 구성 요소 | 역할 및 설명 | 식당 비유 매칭 |
| :--- | :--- | :--- |
| **Command**<br>*(인터페이스/추상 클래스)* | 실행할 작업의 표준 인터페이스 선언 (`execute()`, 선택적으로 `undo()`) | **주문서 양식** |
| **ConcreteCommand**<br>*(구상 커맨드)* | `Command` 인터페이스를 구현하며, 실제 작업을 수행할 `Receiver` 객체와 매개변수를 바인딩 | **작성된 특정 주문서**<br>*(예: 스테이크 주문서)* |
| **Receiver**<br>*(수신자)* | 실제 비즈니스 로직을 수행하는 대상 객체 | **요리사** |
| **Invoker**<br>*(발송자/호출자)* | 커맨드 객체를 저장하고 실행 요청을 내보냄 (예: UI 버튼, 리모컨, 키 매니저) | **웨이터 / 키오스크** |
| **Client**<br>*(클라이언트)* | `ConcreteCommand` 객체를 생성하고 `Receiver` 및 `Invoker`에 연결 | **손님** |

---

## 3. 구조 및 흐름 (Structure & Flow)

### 🏗️ 클래스 관계 및 의존 구조

```text
+------------------+          +------------------------+
|     Client       |--------->| ConcreteCommand        |
+------------------+          +------------------------+
         |                                | 
         | (인스턴스 생성 및 바인딩)     | (구현)
         v                                v
+------------------+          +------------------------+          +------------------+
|     Invoker      |--------->|  <<Interface>> Command |          |     Receiver     |
+------------------+          +------------------------+          +------------------+
| - command        |          | + execute()            |          | + action()       |
| + setCommand()   |          | + undo()               |          +------------------+
| + executeCommand()|         +------------------------+                   ^
+------------------+                      ^                                |
                                          | (실제 작업 위임)                |
                                          +--------------------------------+
```

### 🔄 실행 시 호출 순서 (`->`)

```text
[ Client ] ───(1. Receiver 생성)───────────> [ Receiver ]
   │                                              ▲
   ├───(2. ConcreteCommand 생성 & Receiver 주입)───┤
   │                                              │
   └───(3. Invoker에 Command 등록)───> [ Invoker ]│
                                           │      │
                                (4. execute 호출)  │ (5. action 호출)
                                           ▼      │
                                 [ ConcreteCommand ]
```

---

## 4. 예시: 전등 켜기 (패턴 미적용 vs 적용)

### ❌ 패턴을 적용하지 않았을 때 (기존 방식)
스위치(Invoker)가 전등(Receiver) 클래스에 직접 의존하는 구조입니다.

```typescript
class Light {
    turnOn() { console.log("💡 불이 켜졌습니다."); }
}

class Switch {
    private light: Light; // 전등에 직접 결합됨

    constructor(light: Light) {
        this.light = light;
    }

    press() {
        this.light.turnOn(); // 스위치로 TV, 선풍기 등을 켜려면 Switch 클래스 코드를 다 고쳐야 함!
    }
}
```
* **문제점:** 스위치가 '전등'에 강력하게 묶여 있어, 스위치로 TV나 에어컨을 켜도록 변경하려면 `Switch` 코드를 직접 수정해야 합니다. (확장성 저하, 개방-폐쇄 원칙 위반)

---

### 💡 커맨드 패턴을 적용했을 때
스위치(Invoker)와 전등(Receiver) 사이에 **Command 인터페이스**를 둡니다.

```typescript
// 1. Command 인터페이스
interface Command {
    execute(): void;
    undo(): void;
}

// 2. Receiver (수신자)
class Light {
    turnOn() { console.log("💡 불이 켜졌습니다."); }
    turnOff() { console.log("🔌 불이 꺼졌습니다."); }
}

// 3. ConcreteCommand (구상 커맨드)
class LightOnCommand implements Command {
    constructor(private light: Light) {}

    execute() {
        this.light.turnOn();
    }

    undo() {
        this.light.turnOff();
    }
}

// 4. Invoker (발송자)
class Switch {
    private command?: Command; // 전등을 몰라도 'Command' 인터페이스만 알면 됨!

    setCommand(command: Command) {
        this.command = command;
    }

    press() {
        this.command?.execute();
    }

    pressUndo() {
        this.command?.undo();
    }
}

// 5. Client (사용)
const light = new Light();
const lightOnCommand = new LightOnCommand(light);

const switchButton = new Switch();
switchButton.setCommand(lightOnCommand); // 스위치에 '전등 켜기 커맨드' 장착

switchButton.press();     // 출력: 💡 불이 켜졌습니다.
switchButton.pressUndo(); // 출력: 🔌 불이 꺼졌습니다.
```
* **장점:** `Switch` 코드를 수정하지 않고도 `TVOnCommand`나 `FanOnCommand` 등 새로운 명령을 유연하게 교체 장착할 수 있습니다.

---

## 5. 실무 활용 사례

1. **GUI 에디터/IDE의 Ctrl+Z (Undo / Redo)**
   * 포토샵, VS Code, 워드 프로세서 등에서 사용자가 수행한 작업(텍스트 입력, 도형 그리기, 삭제 등)을 하나하나 `Command` 객체로 **스택(Stack)**에 적재해 두었다가, 취소 요청 시 역순으로 `undo()`를 수행합니다.
2. **트랜잭션 및 작업 큐 (Job Queue / Task Queue)**
   * 이메일 발송, 파일 업로드, 결제 요청 등을 커맨드 객체로 포장하여 작업 큐(Queue)에 적재하고, 백그라운드 워커 프로세스가 순차적으로 꺼내어 실행(`execute()`)합니다.
3. **게임 개발의 입력 매핑 (Input Rebinding)**
   * 키보드/패드 버튼 설정 변경 기능 (예: `Space` 키 -> 점프 또는 공격).
   * 키(Invoker)에 바인딩된 `Command` 객체만 바꾸어 주면 캐릭터(Receiver)의 로직 코드를 수정하지 않고 키 세팅을 다변화할 수 있습니다.

---

## 6. 장점과 단점 (Pros & Cons)

### 👍 장점
* **단일 책임 원칙 (SRP):** 작업 요청자(Invoker)와 작업 수행자(Receiver)를 분리합니다.
* **개방-폐쇄 원칙 (OCP):** 기존 코드를 수정하지 않고 새로운 `Command` 클래스를 추가할 수 있습니다.
* **실행 취소/다시 실행 (Undo/Redo):** 커맨드 상태 저장 및 스택 관리로 손쉽게 구현 가능합니다.
* **작업 예약 및 큐잉:** 요청 자체를 객체화하므로 큐나 스케줄러에 넣어서 지연/순차 실행이 가능합니다.

### 👎 단점
* **클래스 개수 증가:** 개별 작업(Action)마다 구상 커맨드 클래스를 정의해야 하므로 전체 코드베이스 및 클래스 수 증가로 이어질 수 있습니다.

---

## 7. 헷갈리기 쉬운 다른 패턴들과의 차이점

| 패턴 | 관점 및 핵심 목적 | 커맨드 패턴과의 차이점 |
| :--- | :--- | :--- |
| **커맨드 (Command)** | **"무엇을(Action)"** 실행할 것인가 | 요청 자체를 객체화하여 송신자/수신자 결합 해제 및 **실행 취소/대기열 제어**에 집중 |
| **전략 (Strategy)** | **"어떻게(Algorithm)"** 수행할 것인가 | 같은 목적(예: 정렬 방식, 결제 방식)을 수행하는 **알고리즘 교체**에 집중 |
| **책임 연쇄 (Chain of Responsibility)** | **"누가(Handler)"** 처리할 것인가 | 요청을 객체화하기보다 요청이 적절한 처리자를 만날 때까지 **체인을 타고 전달** |
| **메멘토 (Memento)** | **"상태를 어떻게 저장할 것인가"** | 커맨드가 실행 취소를 수행할 때, **객체의 이전 내부 상태를 저장/복원**하기 위해 함께 자주 조합됨 |

---

## 📚 참고자료 (References)
* [Refactoring Guru - 커맨드 패턴](https://refactoring.guru/ko/design-patterns/command)
* [Design Patterns: Elements of Reusable Object-Oriented Software (GoF)]
